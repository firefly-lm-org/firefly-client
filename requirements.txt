# === GPU 训练依赖（torch 必须显式锁版本+索引）=====================
# 安装：pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
# torch cu121 指定 CUDA 12.1；CUDA 11.8 用 cu118
torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121

# === 模型加载与微调 ================================================
transformers==4.46.0
tokenizers==0.20.0
accelerate>=0.34.0
safetensors>=0.4.0
peft>=0.13.0
unsloth==2024.11.0          # 优先用 Unsloth 加速（GPU 内存减半，速度 2×）
trl==0.9.6                  # SFTTrainer
datasets>=2.21.0

# === 客户端原有依赖（mock 也用）====================================
requests==2.32.3
redis==5.0.8
pydantic==2.9.2
click==8.1.7
pytest==8.3.2
rich>=13.0.0
python-dotenv>=1.0.0
typer>=0.12.0
httpx>=0.27.0
