# Firefly LM Client — 依赖清单
# 安装：pip install -r requirements.txt
# 测试（无 GPU）：firefly start --mock
# 训练（需 GPU ≥6GB VRAM）：firefly start

# ── 核心 ML ──────────────────────────────────
# Unsloth 是 QLoRA 训练的核心，自动处理 4-bit 量化 + LoRA 挂载
unsloth>=2024.11.0

# PEFT 提供 LoRA/DoRA 等参数高效微调方法
peft>=0.13.0

# Transformers 底座模型加载与 tokenizer
transformers>=4.46.0

# Torch（CUDA 版本由 Unsloth 自动匹配，CPU 环境也能装但跑不了训练）
torch>=2.4.0

# BitsAndBytes 4-bit 量化后端
bitsandbytes>=0.43.0

# ── 训练加速 / 精度 ──────────────────────────
# Accelerate 多卡 / 混合精度调度（Unsloth 依赖）
accelerate>=0.34.0

# ── 权重序列化 ────────────────────────────────
# safetensors 是 LoRA 权重的标准格式
safetensors>=0.4.0

# ── 数据处理 ──────────────────────────────────
# datasets 用于加载 HuggingFace 数据集
datasets>=2.20.0

# ── 训练框架辅助 ──────────────────────────────
# TRL 提供 SFTTrainer（Unsloth 包装用）
trl>=0.9.0

# ── HTTP / 客户端 ─────────────────────────────
# 调度中心通信
httpx>=0.27.0
requests>=2.32.0

# ── 工具 ──────────────────────────────────────
pydantic>=2.0
python-dotenv>=1.0
tqdm>=4.66
loguru>=0.7
PyYAML>=6.0

# ── 测试 ──────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
mock>=5.1
